package rules

// PreferAddGitRuleCode is the full rule code for the prefer-add-git rule.
// Used by other rules to let git-source rewrites own matching RUN instructions.
const PreferAddGitRuleCode = TallyRulePrefix + "prefer-add-git"
