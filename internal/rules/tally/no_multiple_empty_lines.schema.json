{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tally.wharflab.com/rules/tally/no_multiple_empty_lines.schema.json",
  "title": "tally/no-multiple-empty-lines rule config",
  "description": "Configuration options for the tally/no-multiple-empty-lines rule.",
  "type": "object",
  "properties": {
    "severity": { "$ref": "../rule-config.schema.json#/$defs/severity" },
    "fix": { "$ref": "../rule-config.schema.json#/$defs/fix" },
    "exclude": { "$ref": "../rule-config.schema.json#/$defs/exclude" },
    "max": {
      "type": "integer",
      "minimum": 0,
      "default": 1,
      "description": "Maximum number of consecutive empty lines allowed anywhere in the file.",
      "examples": [1, 2]
    },
    "max-bof": {
      "type": "integer",
      "minimum": 0,
      "default": 0,
      "description": "Maximum number of consecutive empty lines allowed at the beginning of the file.",
      "examples": [0, 1]
    },
    "max-eof": {
      "type": "integer",
      "minimum": 0,
      "default": 0,
      "description": "Maximum number of consecutive empty lines allowed at the end of the file.",
      "examples": [0, 1]
    }
  },
  "additionalProperties": false,
  "examples": [
    { "max": 2 },
    { "max": 1, "max-bof": 0, "max-eof": 0 }
  ]
}
