unsafe-fixes = true

[slow-checks]
mode = "off"

[rules]
include = ["tally/prefer-copy-heredoc", "tally/prefer-run-heredoc", "tally/prefer-package-cache-mounts", "tally/prefer-curl-config"]
exclude = ["*"]
