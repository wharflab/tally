unsafe-fixes = true

[slow-checks]
mode = "off"

[rules]
include = ["tally/consistent-indentation", "tally/prefer-run-heredoc"]
exclude = ["*"]

[rules.tally.consistent-indentation]
severity = "style"
