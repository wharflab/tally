unsafe-fixes = true

[slow-checks]
mode = "off"

[rules]
include = ["tally/powershell/error-action-preference", "tally/powershell/prefer-shell-instruction"]
exclude = ["*"]

[output]
fail-level = "none"
