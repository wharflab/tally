unsafe-fixes = true

[slow-checks]
mode = "off"

[rules]
include = ["tally/labels/no-buildx-git-overlap"]
exclude = ["*"]

[output]
fail-level = "none"

[rules.tally.labels.no-buildx-git-overlap]
buildx-git-labels = "full"
