[slow-checks]
mode = "off"

[rules]
include = ["buildkit/InvalidDefinitionDescription", "buildkit/StageNameCasing", "buildkit/MaintainerDeprecated", "buildkit/JSONArgsRecommended"]
exclude = ["*"]

[output]
format = "sarif"
