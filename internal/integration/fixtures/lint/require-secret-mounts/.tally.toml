[slow-checks]
mode = "off"

[rules]
include = ["tally/require-secret-mounts"]
exclude = ["*"]

[rules.tally.require-secret-mounts]
severity = "warning"

[rules.tally.require-secret-mounts.commands.pip]
id = "pipconf"
target = "/root/.config/pip/pip.conf"
