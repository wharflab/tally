---
# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
id: shellcheck-drop-sc2187
message: Remove SC2187 emission
severity: info
language: Haskell
rule:
  pattern:
    context: |
      module Test where
      foo = do
          when (executableFromShebang sb == "ash") $
              warn $ID 2187 $MSG
    selector: exp
fix: ''
